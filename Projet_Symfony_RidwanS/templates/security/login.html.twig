{# Fichier twig de la page de connexion #}
{# Cette page est générée automatiquement avec la commande "make:auth" #}
{% extends 'base.html.twig' %}

{% block title %}GR Shop{% endblock %}

{% block body %}
<form method="post">
    {% if error %}
        <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}

    <br>
    <h1>{{ 'compte.id'|trans }}</h1>

    {% if app.user %}
        <div class="mb-3">
            <h5>{{'compte.connecte'|trans}} {{ app.user.username }}, <a href="{{ path('app_logout') }}">{{'compte.logout'|trans}}</a></h5>
        </div>
    {% endif %}

    <label for="inputEmail">Email</label>
    <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control" required autofocus>
    <br/>
    <label for="inputPassword">{{'compte.mdp'|trans}}</label>
    <input type="password" name="password" id="inputPassword" class="form-control" required>

    <br/>

    <input type="hidden" name="_csrf_token"
           value="{{ csrf_token('authenticate') }}"
    >

    {#
        Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
        See https://symfony.com/doc/current/security/remember_me.html

        <div class="checkbox mb-3">
            <label>
                <input type="checkbox" name="_remember_me"> Remember me
            </label>
        </div>
    #}

    <button class="btn btn-lg btn-primary" type="submit">
        {{'compte.login'|trans}}
    </button>
</form>
{% endblock %}
